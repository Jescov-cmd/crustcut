<UserControl x:Class="PrimeOSTuner.UI.Controls.StatCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">
    <Border Style="{StaticResource CardBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="NameText" Grid.Row="0" Style="{StaticResource SectionLabel}"/>
            <TextBlock x:Name="ValueTextBlock" Grid.Row="1" FontSize="28" FontWeight="Bold" Foreground="{StaticResource Text0Brush}"/>
            <TextBlock x:Name="SubTextBlock" Grid.Row="2" FontSize="11" Foreground="{StaticResource Text3Brush}"/>
            <lvc:CartesianChart x:Name="Spark" Grid.Row="3" Height="40" Margin="0,8,0,0"
                                AnimationsSpeed="0:0:0.3"
                                TooltipPosition="Hidden"/>
        </Grid>
    </Border>
</UserControl>
