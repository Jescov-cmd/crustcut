<UserControl x:Class="PrimeOSTuner.UI.Controls.StatCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="NameText" Grid.Row="0" Style="{StaticResource SectionLabel}"/>
            <TextBlock x:Name="ValueTextBlock" Grid.Row="1" FontSize="28" FontWeight="Bold"
                       Foreground="{StaticResource AccentBrush}"/>
            <TextBlock x:Name="SubTextBlock" Grid.Row="2" FontSize="11" Opacity="0.55"/>
            <Canvas x:Name="SparkHost" Grid.Row="3" Height="40" Margin="0,8,0,0" ClipToBounds="True">
                <Path x:Name="SparkFill" Fill="#33F77254"/>
                <Path x:Name="SparkLine" Stroke="{StaticResource AccentBrush}"
                      StrokeThickness="2" StrokeLineJoin="Round"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
            </Canvas>
        </Grid>
    </Border>
</UserControl>
