<UserControl x:Class="PrimeOSTuner.UI.Controls.GameCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             Width="220" Height="320" Cursor="Hand">
    <UserControl.Resources>
        <!-- Profile mode keys in display order -->
        <x:Array x:Key="ProfileModes" Type="{x:Type sys:String}">
            <sys:String>(none)</sys:String>
            <sys:String>basic</sys:String>
            <sys:String>performance</sys:String>
            <sys:String>custom</sys:String>
        </x:Array>

        <!-- Pill item — matches the Memory Priority segmented control -->
        <Style x:Key="ProfilePillItemStyle" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{StaticResource Text2Brush}"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="bg"
                                CornerRadius="6"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="1"
                                Margin="2,0"
                                Padding="6,5">
                            <TextBlock Text="{Binding Converter={StaticResource ProfileModeShortLabel}}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#15FFFFFF"/>
                                <Setter Property="Foreground" Value="{StaticResource Text0Brush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="{StaticResource AccentGradientBrush}"/>
                                <Setter TargetName="bg" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="#1A1A1A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ProfilePillsListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource Bg1Brush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource StrokeBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ProfilePillItemStyle}"/>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="1"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource Bg2Brush}" CornerRadius="12" BorderBrush="{StaticResource StrokeBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Border x:Name="Skeleton"
                        Background="{StaticResource Bg2Brush}" CornerRadius="12,12,0,0"
                        Visibility="{Binding IsLoadingCover, Converter={StaticResource BoolToVisibility}}"/>
                <Image x:Name="Cover"
                       Source="{Binding CoverImagePath}"
                       Stretch="UniformToFill"/>
                <Border Background="#80000000" VerticalAlignment="Bottom" Height="80" CornerRadius="0,0,0,0"/>
                <StackPanel VerticalAlignment="Bottom" Margin="10,0,10,10">
                    <TextBlock Text="{Binding DisplayName}" Foreground="{StaticResource Text0Brush}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding AssignedMode}" Foreground="{StaticResource AccentBrush}" FontSize="11"/>
                </StackPanel>
            </Grid>

            <ListBox Grid.Row="1"
                     x:Name="ProfilePills"
                     Style="{StaticResource ProfilePillsListBoxStyle}"
                     ItemsSource="{StaticResource ProfileModes}"
                     SelectionChanged="ProfilePills_SelectionChanged"
                     Margin="8"/>
        </Grid>
    </Border>
</UserControl>
