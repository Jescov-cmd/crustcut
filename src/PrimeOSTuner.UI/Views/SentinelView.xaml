<UserControl x:Class="PrimeOSTuner.UI.Views.SentinelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="MetricRowCard" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsProblem}" Value="True">
                    <Setter Property="BorderBrush" Value="#FF6A6A"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <DataTemplate x:Key="SentinelMetricRow">
            <Border Style="{StaticResource MetricRowCard}">
                <StackPanel>
                    <TextBlock Text="{Binding Label}" FontWeight="SemiBold" FontSize="14"/>
                    <TextBlock Text="{Binding Current}" Foreground="{StaticResource Text2Brush}" FontSize="12"/>
                    <TextBlock Text="{Binding Recommended}" Foreground="{StaticResource Text3Brush}" FontSize="11" Margin="0,2,0,0"/>
                    <TextBlock Text="{Binding Explanation}" Foreground="#FFB0B0" FontSize="11" Margin="0,6,0,0"
                               TextWrapping="Wrap"
                               Visibility="{Binding IsProblem, Converter={StaticResource BoolToVisibility}}"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,16,0">
        <StackPanel Margin="0,0,0,32">
            <TextBlock Text="Sentinel" Style="{StaticResource HeaderText}" Margin="0,0,0,8"/>
            <TextBlock Text="{Binding Status}" Style="{StaticResource SubHeaderText}" Margin="0,0,0,20"/>

            <!-- Enable toggle (moved here from Settings) -->
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Performance watcher" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Text="Watches your running game's live resource use and records its FPS. When the game closes, the results appear below."
                                   Foreground="{StaticResource Text2Brush}" FontSize="12"
                                   TextWrapping="Wrap" Margin="0,2,0,0"/>
                    </StackPanel>
                    <ToggleButton Grid.Column="1" Style="{StaticResource ToggleSwitchStyle}"
                                  IsChecked="{Binding Enabled, Mode=TwoWay}"
                                  VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Last game performance (avg / highest / 1% low FPS) -->
            <TextBlock Text="LAST GAME PERFORMANCE" Style="{StaticResource SectionLabel}"/>
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,18">
                <StackPanel>
                    <StackPanel Visibility="{Binding HasLastSession, Converter={StaticResource BoolToVisibility}}">
                        <TextBlock Text="{Binding LastGameName}" FontWeight="SemiBold" FontSize="16"/>
                        <TextBlock Text="{Binding LastWhen}" Foreground="{StaticResource Text3Brush}"
                                   FontSize="11" Margin="0,2,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="AVERAGE" Foreground="{StaticResource Text3Brush}" FontSize="11" FontWeight="Bold"/>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock Text="{Binding LastAvgFps}" FontSize="34" FontWeight="Bold"
                                               Foreground="{StaticResource AccentBrush}"/>
                                    <TextBlock Text="FPS" Foreground="{StaticResource Text3Brush}" FontSize="12"
                                               VerticalAlignment="Bottom" Margin="4,0,0,6"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="HIGHEST" Foreground="{StaticResource Text3Brush}" FontSize="11" FontWeight="Bold"/>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock Text="{Binding LastMaxFps}" FontSize="34" FontWeight="Bold"
                                               Foreground="{StaticResource Text0Brush}"/>
                                    <TextBlock Text="FPS" Foreground="{StaticResource Text3Brush}" FontSize="12"
                                               VerticalAlignment="Bottom" Margin="4,0,0,6"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="1% LOW" Foreground="{StaticResource Text3Brush}" FontSize="11" FontWeight="Bold"/>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock Text="{Binding LastOnePctLow}" FontSize="34" FontWeight="Bold"
                                               Foreground="{StaticResource Text0Brush}"/>
                                    <TextBlock Text="FPS" Foreground="{StaticResource Text3Brush}" FontSize="12"
                                               VerticalAlignment="Bottom" Margin="4,0,0,6"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                    <TextBlock Text="Play a game with the watcher on — when you exit, your average, highest, and 1% low FPS will show up here."
                               Foreground="{StaticResource Text2Brush}" FontSize="12" TextWrapping="Wrap"
                               Visibility="{Binding HasLastSession, Converter={StaticResource InverseBoolToVisibility}}"/>
                </StackPanel>
            </Border>

            <TextBlock Text="LIVE METRICS" Style="{StaticResource SectionLabel}"/>

            <ContentControl Content="{Binding Vram}" ContentTemplate="{StaticResource SentinelMetricRow}" Margin="0,0,0,8"/>
            <ContentControl Content="{Binding Ram}"  ContentTemplate="{StaticResource SentinelMetricRow}" Margin="0,0,0,8"/>
            <ContentControl Content="{Binding Cpu}"  ContentTemplate="{StaticResource SentinelMetricRow}" Margin="0,0,0,18"/>

            <TextBlock Text="RECENT ALERTS (THIS SESSION)" Style="{StaticResource SectionLabel}"/>
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,18">
                <ItemsControl ItemsSource="{Binding RecentAlerts}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{Binding DetectedAt, StringFormat='{}{0:HH:mm:ss}'}"
                                           Foreground="{StaticResource Text3Brush}"
                                           FontSize="11" Margin="0,0,10,0"/>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding Detail}"
                                           Foreground="{StaticResource Text0Brush}"
                                           FontSize="12" TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>

            <TextBlock TextWrapping="Wrap" Foreground="{StaticResource Text3Brush}" FontSize="11">
                Sentinel watches passively. No notifications, no popups. A red dot on the tab is the only signal — open the tab to see what's wrong.
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</UserControl>
