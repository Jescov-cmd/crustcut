<UserControl x:Class="PrimeOSTuner.UI.Views.OptimizeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Optimize" Style="{StaticResource HeaderText}" Margin="0,0,0,12"/>

        <!-- Filter chip bar -->
        <ItemsControl Grid.Row="1" x:Name="FilterChips" Margin="0,0,0,18">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ToggleButton Style="{StaticResource FilterChip}"
                                  Content="{Binding Label}"
                                  IsChecked="{Binding IsActive, Mode=TwoWay}"
                                  Tag="{Binding Key}"
                                  Click="ChipClick"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="TweakList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorder}" Padding="22,16" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DisplayName}"
                                               Foreground="{StaticResource Text0Brush}"
                                               FontWeight="SemiBold" FontSize="15"
                                               VerticalAlignment="Center"/>
                                    <Border Style="{StaticResource InfoBadge}"
                                            ToolTip="{Binding Description}">
                                        <TextBlock Text="i" FontStyle="Italic" FontWeight="Bold"
                                                   Foreground="{StaticResource Text1Brush}"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   FontSize="11"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="Preview" Tag="{Binding}" Click="PreviewClick"
                                            Padding="14,8" Margin="0,0,8,0"
                                            Background="Transparent"
                                            Foreground="{StaticResource Text1Brush}"
                                            BorderBrush="{StaticResource LineBrush}" BorderThickness="1"
                                            FontSize="12" Cursor="Hand"/>
                                    <Button Style="{StaticResource PrimaryActionButton}"
                                            Content="Apply" Tag="{Binding}" Click="ApplyClick"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
