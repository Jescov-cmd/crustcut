<UserControl x:Class="PrimeOSTuner.UI.Views.MaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,16,0">
        <StackPanel Margin="0,0,0,32">
            <TextBlock Text="Maintenance" Style="{StaticResource HeaderText}" Margin="0,0,0,8"/>
            <TextBlock Text="Disk space and housekeeping"
                       Style="{StaticResource SubHeaderText}" Margin="0,0,0,16"/>

            <TextBlock TextWrapping="Wrap" Foreground="{StaticResource Text3Brush}"
                       FontSize="12" Margin="0,0,0,20">
                These actions free disk space or clear caches to keep Windows tidy. They are
                housekeeping, not performance tweaks — they won't raise FPS. Run them occasionally.
            </TextBlock>

            <WrapPanel HorizontalAlignment="Left">
                <ItemsControl x:Name="CleanupActions">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardBorder}" Width="280" Height="180" Margin="0,0,12,12">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="MAINTENANCE" Style="{StaticResource SectionLabel}"/>
                                    <StackPanel Grid.Row="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DisplayName}"
                                                   FontFamily="Segoe UI Variable Display, Segoe UI"
                                                   FontSize="16" FontWeight="SemiBold"
                                                   TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding Description}"
                                                   Foreground="{StaticResource Text2Brush}"
                                                   FontSize="11" Margin="0,4,0,0"
                                                   TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                                                   MaxHeight="48"/>
                                    </StackPanel>
                                    <Button Grid.Row="2"
                                            Content="Run"
                                            Tag="{Binding}"
                                            Click="CleanupClick"
                                            HorizontalAlignment="Stretch"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </WrapPanel>

            <TextBlock x:Name="CleanupStatus"
                       Foreground="{StaticResource Text3Brush}"
                       FontSize="11" Margin="0,12,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
