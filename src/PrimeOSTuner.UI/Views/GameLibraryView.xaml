<UserControl x:Class="PrimeOSTuner.UI.Views.GameLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:PrimeOSTuner.UI.Controls">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,16,0">
        <StackPanel Margin="0,0,0,32">
            <TextBlock Text="Library" Style="{StaticResource HeaderText}" Margin="0,0,0,8"/>
            <TextBlock Text="Attach a profile to each game so it applies automatically on launch"
                       Style="{StaticResource SubHeaderText}" Margin="0,0,0,24"/>

            <!-- Top action row -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center"
                           Foreground="{StaticResource Text2Brush}"
                           FontSize="12"
                           Text="Click + Add Game to register an executable. Cover art comes from SteamGridDB."/>
                <Button Grid.Column="1"
                        Style="{StaticResource PrimaryActionButton}"
                        Content="+ Add Game"
                        Click="AddGameClick"/>
            </Grid>

            <!-- API key prompt card (full-width) -->
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,16"
                    Visibility="{Binding ShowApiKeyPrompt, Converter={StaticResource BoolToVisibility}}">
                <StackPanel>
                    <TextBlock Text="Cover art disabled"
                               Foreground="{StaticResource WarnBrush}"
                               FontWeight="SemiBold" FontSize="14"/>
                    <TextBlock Text="Add a SteamGridDB API key to %LOCALAPPDATA%\PrimeOSTuner\settings.json under SteamGridDbApiKey to enable cover art."
                               Foreground="{StaticResource Text2Brush}"
                               FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Tile grid: WrapPanel is appropriate here because tiles are genuine fixed-width cards -->
            <ItemsControl ItemsSource="{Binding Tiles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <c:GameCard Margin="0,0,12,12" ProfileChanged="GameCard_ProfileChanged"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
