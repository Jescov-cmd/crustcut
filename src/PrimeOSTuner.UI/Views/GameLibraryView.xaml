<UserControl x:Class="PrimeOSTuner.UI.Views.GameLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:PrimeOSTuner.UI.Controls">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,16,0">
        <StackPanel Margin="0,0,0,32">
            <TextBlock Text="Library" Style="{StaticResource HeaderText}" Margin="0,0,0,8"/>
            <TextBlock Text="Attach a profile to each game so it applies automatically on launch"
                       Style="{StaticResource SubHeaderText}" Margin="0,0,0,24"/>

            <!-- Top action row -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center"
                           Foreground="{StaticResource Text2Brush}"
                           FontSize="12"
                           Text="Click + Add Game to register an executable. Cover art is fetched automatically for Steam games."/>
                <Button Grid.Column="1"
                        Style="{StaticResource PrimaryActionButton}"
                        Content="+ Add Game"
                        Click="AddGameClick"/>
            </Grid>

            <!-- Cover-art prompt for games we couldn't auto-link to Steam. Steam itself
                 doesn't expose art for games not in its catalog, so we fall back to
                 SteamGridDB — a separate community art database, unrelated to Valve. -->
            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,16"
                    Visibility="{Binding ShowApiKeyPrompt, Converter={StaticResource BoolToVisibility}}">
                <StackPanel>
                    <TextBlock Text="Cover art for games not on Steam"
                               Foreground="{StaticResource WarnBrush}"
                               FontWeight="SemiBold" FontSize="14"/>
                    <TextBlock TextWrapping="Wrap" Margin="0,4,0,0" FontSize="12"
                               Foreground="{StaticResource Text2Brush}">
                        <Run Text="Most games — including manually-added ones — get covers automatically from Steam's public CDN."/>
                        <LineBreak/>
                        <LineBreak/>
                        <Run Text="For titles that aren't on Steam at all, "/>
                        <Run Text="SteamGridDB" FontWeight="SemiBold" Foreground="{StaticResource Text0Brush}"/>
                        <Run Text=" (a community art database, not run by Steam/Valve) has covers — but it needs a free API key from steamgriddb.com. Save it as "/>
                        <Run Text="SteamGridDbApiKey" FontFamily="Consolas" Foreground="{StaticResource Text0Brush}"/>
                        <Run Text=" in "/>
                        <Run Text="%LOCALAPPDATA%\PrimeOSTuner\settings.json" FontFamily="Consolas" Foreground="{StaticResource Text0Brush}"/>
                        <Run Text="."/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Tile grid: WrapPanel is appropriate here because tiles are genuine fixed-width cards -->
            <ItemsControl ItemsSource="{Binding Tiles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <c:GameCard Margin="0,0,12,12" ProfileChanged="GameCard_ProfileChanged"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
