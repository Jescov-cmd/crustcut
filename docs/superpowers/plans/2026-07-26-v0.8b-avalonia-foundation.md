# v0.8b — Avalonia Foundation & Redesigned Overview

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up the Avalonia application alongside the untouched WPF app, with the design-token system, the repositioned navigation, and the redesigned Overview page running on live data.

**Architecture:** Two new projects. `Crustcut.Presentation` holds view-models with no UI-framework dependency, replacing direct `System.Windows.Application.Current.Dispatcher` calls with an injected `IUiDispatcher`. `Crustcut.App` is the Avalonia host. `PrimeOSTuner.UI` (WPF) is frozen and untouched, so the app remains runnable at every commit and the existing suite stays green.

**Tech Stack:** C# / .NET 9 / Avalonia 12.1.0 / CommunityToolkit.Mvvm / xUnit / Moq / FluentAssertions.

## Global Constraints

- All new projects target **`net9.0-windows`**. The `dotnet new avalonia.*` templates scaffold `net10.0`, which SDK 9.0.313 **cannot build** — retarget immediately after scaffolding.
- **Avalonia version is `12.1.0`** across every package reference.
- The template scaffold references `AvaloniaUI.DiagnosticsSupport` and `Avalonia.Fonts.Inter`. If either package is removed, its matching `AppBuilder` call (`.WithDeveloperTools()` / `.WithInterFont()`) **must** be removed too, or the build fails with CS1061.
- `Window.SystemDecorations` is **obsolete** in Avalonia 12 — use `WindowDecorations`.
- **Do not modify anything under `src/PrimeOSTuner.UI/`.** It is frozen until parity, then deleted in a later plan.
- No literal colours, radii, or font sizes in view markup. Everything resolves from `Theme/Tokens.axaml`.
- Copy must present Crustcut as general performance software. Never "games" where "apps" is accurate.
- Baseline before starting: **417 passing, 0 failing** (`dotnet test src/PrimeOSTuner.Tests/PrimeOSTuner.Tests.csproj`).
- Do not bump the version number in any task.

---

### Task 1: `Crustcut.Presentation` with `IUiDispatcher` and the navigation model

**Files:**
- Create: `src/Crustcut.Presentation/Crustcut.Presentation.csproj`
- Create: `src/Crustcut.Presentation/IUiDispatcher.cs`
- Create: `src/Crustcut.Presentation/Navigation/NavItem.cs`
- Create: `src/Crustcut.Presentation/Navigation/NavCatalog.cs`
- Create: `src/Crustcut.Presentation/ShellViewModel.cs`
- Test: `src/PrimeOSTuner.Tests/Presentation/ShellViewModelTests.cs`
- Modify: `PrimeOSTuner.sln`, `src/PrimeOSTuner.Tests/PrimeOSTuner.Tests.csproj`

**Interfaces:**
- Consumes: nothing.
- Produces:
  - `interface IUiDispatcher { void Post(Action action); }`
  - `sealed record NavItem(string Id, string Label, string Group)`
  - `static class NavCatalog` with `IReadOnlyList<NavItem> Primary` and `IReadOnlyList<NavItem> Bottom`
  - `ShellViewModel` with `string ActiveTab`, `IReadOnlyList<NavItem> Primary`, `IReadOnlyList<NavItem> Bottom`, `IRelayCommand<string> NavigateCommand`, `bool IsActive(string id)`

- [ ] **Step 1: Create the project**

```bash
dotnet new classlib -o src/Crustcut.Presentation -f net9.0
dotnet sln PrimeOSTuner.sln add src/Crustcut.Presentation/Crustcut.Presentation.csproj
```

Replace `src/Crustcut.Presentation/Crustcut.Presentation.csproj` with:

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net9.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\PrimeOSTuner.Core\PrimeOSTuner.Core.csproj" />
  </ItemGroup>
</Project>
```

Delete the generated `Class1.cs`.

Add the project reference to the test project — inside the existing `<ItemGroup>` that holds the other `ProjectReference` entries in `src/PrimeOSTuner.Tests/PrimeOSTuner.Tests.csproj`:

```xml
<ProjectReference Include="..\Crustcut.Presentation\Crustcut.Presentation.csproj" />
```

- [ ] **Step 2: Write `IUiDispatcher`**

Create `src/Crustcut.Presentation/IUiDispatcher.cs`:

```csharp
namespace Crustcut.Presentation;

/// <summary>
/// Marshals a callback onto the UI thread. Exists so view-models never reference a UI
/// framework directly — the WPF build reached for System.Windows.Application.Current.Dispatcher,
/// which both tied view-models to WPF and made them untestable.
/// </summary>
public interface IUiDispatcher
{
    /// <summary>Queues <paramref name="action"/> to run on the UI thread.</summary>
    void Post(Action action);
}
```

- [ ] **Step 3: Write the navigation model**

Create `src/Crustcut.Presentation/Navigation/NavItem.cs`:

```csharp
namespace Crustcut.Presentation.Navigation;

/// <summary>One entry in the navigation rail.</summary>
/// <param name="Id">Stable key used for routing. Never shown to the user.</param>
/// <param name="Label">Display text.</param>
/// <param name="Group">Section heading, or empty for an ungrouped item.</param>
public sealed record NavItem(string Id, string Label, string Group);
```

Create `src/Crustcut.Presentation/Navigation/NavCatalog.cs`:

```csharp
namespace Crustcut.Presentation.Navigation;

/// <summary>
/// The repositioned information architecture. Gaming is one section of a performance tool
/// rather than the frame of the whole product.
/// </summary>
public static class NavCatalog
{
    public const string Performance = "PERFORMANCE";
    public const string Gaming      = "GAMING";
    public const string Learn       = "LEARN";

    public static readonly IReadOnlyList<NavItem> Primary = new[]
    {
        new NavItem("Overview",  "Overview",   ""),

        new NavItem("Optimize",  "Optimize",   Performance),
        new NavItem("Diagnosis", "Diagnosis",  Performance),
        new NavItem("Cleanup",   "Cleanup",    Performance),   // was "Bloatware"
        new NavItem("Memory",    "Memory",     Performance),

        new NavItem("Games",     "Games",      Gaming),        // was "Library"
        new NavItem("Sessions",  "Sessions",   Gaming),        // was "Sentinel"
        new NavItem("GameBoost", "Game Boost", Gaming),

        new NavItem("Guides",    "Guides",     Learn),
    };

    public static readonly IReadOnlyList<NavItem> Bottom = new[]
    {
        new NavItem("History",  "History",  ""),
        new NavItem("Settings", "Settings", ""),
    };
}
```

- [ ] **Step 4: Write the failing tests**

Create `src/PrimeOSTuner.Tests/Presentation/ShellViewModelTests.cs`:

```csharp
using Crustcut.Presentation;
using Crustcut.Presentation.Navigation;
using FluentAssertions;
using Xunit;

namespace PrimeOSTuner.Tests.Presentation;

public class ShellViewModelTests
{
    [Fact]
    public void Starts_on_Overview()
    {
        new ShellViewModel().ActiveTab.Should().Be("Overview");
    }

    [Fact]
    public void Navigate_changes_the_active_tab()
    {
        var vm = new ShellViewModel();

        vm.NavigateCommand.Execute("Cleanup");

        vm.ActiveTab.Should().Be("Cleanup");
        vm.IsActive("Cleanup").Should().BeTrue();
        vm.IsActive("Overview").Should().BeFalse();
    }

    [Fact]
    public void Navigate_ignores_an_unknown_tab()
    {
        var vm = new ShellViewModel();

        vm.NavigateCommand.Execute("NotARealTab");

        vm.ActiveTab.Should().Be("Overview");
    }

    [Fact]
    public void Every_nav_id_is_unique()
    {
        var ids = NavCatalog.Primary.Concat(NavCatalog.Bottom).Select(i => i.Id).ToList();

        ids.Should().OnlyHaveUniqueItems();
    }

    [Fact]
    public void Gaming_is_a_section_rather_than_the_frame()
    {
        // Repositioning guard: gaming must not be the only or dominant grouping.
        var performance = NavCatalog.Primary.Count(i => i.Group == NavCatalog.Performance);
        var gaming      = NavCatalog.Primary.Count(i => i.Group == NavCatalog.Gaming);

        performance.Should().BeGreaterThanOrEqualTo(gaming);
    }

    [Fact]
    public void Retired_gaming_first_labels_are_gone()
    {
        var labels = NavCatalog.Primary.Concat(NavCatalog.Bottom).Select(i => i.Label).ToList();

        labels.Should().NotContain("Bloatware");
        labels.Should().NotContain("Library");
        labels.Should().NotContain("Sentinel");
        labels.Should().NotContain("Dashboard");
    }
}
```

- [ ] **Step 5: Run the tests to verify they fail**

Run: `dotnet test src/PrimeOSTuner.Tests/PrimeOSTuner.Tests.csproj --filter "FullyQualifiedName~ShellViewModelTests" --nologo`

Expected: FAIL to compile — `Crustcut.Presentation.ShellViewModel` does not exist yet.

- [ ] **Step 6: Write `ShellViewModel`**

Create `src/Crustcut.Presentation/ShellViewModel.cs`:

```csharp
using CommunityToolkit.Mvvm.ComponentModel;
using CommunityToolkit.Mvvm.Input;
using Crustcut.Presentation.Navigation;

namespace Crustcut.Presentation;

public partial class ShellViewModel : ObservableObject
{
    public IReadOnlyList<NavItem> Primary => NavCatalog.Primary;
    public IReadOnlyList<NavItem> Bottom  => NavCatalog.Bottom;

    [ObservableProperty] private string _activeTab = "Overview";

    public bool IsActive(string id) => string.Equals(ActiveTab, id, StringComparison.Ordinal);

    [RelayCommand]
    private void Navigate(string? tab)
    {
        if (string.IsNullOrWhiteSpace(tab)) return;
        // Ignore ids that aren't in the catalog, so a typo in markup can't blank the page.
        var known = NavCatalog.Primary.Concat(NavCatalog.Bottom).Any(i => i.Id == tab);
        if (!known) return;
        ActiveTab = tab;
    }
}
```

- [ ] **Step 7: Run the tests to verify they pass**

Run: `dotnet test src/PrimeOSTuner.Tests/PrimeOSTuner.Tests.csproj --filter "FullyQualifiedName~ShellViewModelTests" --nologo`

Expected: PASS, 6 tests.

- [ ] **Step 8: Run the whole suite and commit**

Run: `dotnet test src/PrimeOSTuner.Tests/PrimeOSTuner.Tests.csproj --nologo` — expect **423** passing.

```bash
git add src/Crustcut.Presentation src/PrimeOSTuner.Tests/Presentation PrimeOSTuner.sln src/PrimeOSTuner.Tests/PrimeOSTuner.Tests.csproj
git commit -m "feat(v0.8b): Crustcut.Presentation with IUiDispatcher and repositioned nav

View-models move to a project with no UI-framework dependency. NavCatalog encodes
the new information architecture: gaming becomes one section of a performance tool.
Bloatware -> Cleanup, Library -> Games, Sentinel -> Sessions, Dashboard -> Overview."
```

---

### Task 2: `Crustcut.App` Avalonia host that builds and runs

**Files:**
- Create: `src/Crustcut.App/` (scaffolded, then edited)
- Modify: `PrimeOSTuner.sln`

**Interfaces:**
- Consumes: `ShellViewModel` from Task 1.
- Produces: a runnable Avalonia executable named `Crustcut.App`, with `AvaloniaDispatcher : IUiDispatcher`.

- [ ] **Step 1: Scaffold and retarget**

```bash
dotnet new avalonia.app -o src/Crustcut.App
dotnet sln PrimeOSTuner.sln add src/Crustcut.App/Crustcut.App.csproj
```

Replace `src/Crustcut.App/Crustcut.App.csproj` with:

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net9.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <BuiltInComInteropSupport>true</BuiltInComInteropSupport>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="12.1.0" />
    <PackageReference Include="Avalonia.Desktop" Version="12.1.0" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="12.1.0" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="8.0.1" />
    <PackageReference Include="Serilog.Extensions.Hosting" Version="8.0.0" />
    <PackageReference Include="Serilog.Sinks.File" Version="6.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Crustcut.Presentation\Crustcut.Presentation.csproj" />
    <ProjectReference Include="..\PrimeOSTuner.Core\PrimeOSTuner.Core.csproj" />
    <ProjectReference Include="..\PrimeOSTuner.Win\PrimeOSTuner.Win.csproj" />
  </ItemGroup>
</Project>
```

Note the dropped packages. In `src/Crustcut.App/Program.cs`, remove the matching `AppBuilder` calls so the reduced `BuildAvaloniaApp` reads:

```csharp
public static AppBuilder BuildAvaloniaApp()
    => AppBuilder.Configure<App>()
        .UsePlatformDetect()
        .LogToTrace();
```

Copy `src/PrimeOSTuner.UI/app.manifest` to `src/Crustcut.App/app.manifest` so the new executable requests administrator in the same way.

- [ ] **Step 2: Write `AvaloniaDispatcher`**

Create `src/Crustcut.App/Services/AvaloniaDispatcher.cs`:

```csharp
using Avalonia.Threading;
using Crustcut.Presentation;

namespace Crustcut.App.Services;

public sealed class AvaloniaDispatcher : IUiDispatcher
{
    public void Post(Action action) => Dispatcher.UIThread.Post(action);
}
```

- [ ] **Step 3: Build and run**

```bash
dotnet build src/Crustcut.App/Crustcut.App.csproj -c Debug --nologo
```

Expected: SUCCEED, 0 errors. Running it should show the default Avalonia window; close it.

- [ ] **Step 4: Commit**

```bash
git add src/Crustcut.App PrimeOSTuner.sln
git commit -m "feat(v0.8b): Avalonia host project scaffolded and building

Targets net9.0-windows (the template scaffolds net10.0, which SDK 9 cannot build).
Drops the diagnostics and Inter-font packages along with their AppBuilder calls."
```

---

### Task 3: Design tokens

**Files:**
- Create: `src/Crustcut.App/Theme/Tokens.axaml`
- Modify: `src/Crustcut.App/App.axaml`

**Interfaces:**
- Consumes: nothing.
- Produces: resource keys used by every later view — `SurfaceBase`, `SurfaceCard`, `SurfaceRail`, `Text0`–`Text3`, `AccentWheat`, `AccentCrust`, `AccentGradient`, `Good`, `Warn`, `Danger`, `RadiusCard`, `RadiusPill`, `FontDisplay`, `FontTitle`, `FontBody`, `FontLabel`, `SpaceSm`, `SpaceMd`, `SpaceLg`.

- [ ] **Step 1: Write the token dictionary**

Create `src/Crustcut.App/Theme/Tokens.axaml`:

```xml
<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- Single source of truth for the visual language. No view may hardcode a colour,
       radius, or font size. This is what keeps a future restyle (and the macOS port)
       cheap. -->

  <!-- Surfaces: near-black base, translucent cards, no hard borders. -->
  <SolidColorBrush x:Key="SurfaceBase"  Color="#0C0B0A"/>
  <SolidColorBrush x:Key="SurfaceRail"  Color="#100F0E"/>
  <SolidColorBrush x:Key="SurfaceCard"  Color="#0EFFFFFF"/>
  <SolidColorBrush x:Key="SurfaceHover" Color="#18FFFFFF"/>
  <SolidColorBrush x:Key="Hairline"     Color="#14FFFFFF"/>

  <!-- Text ramp: warm off-white through to muted. -->
  <SolidColorBrush x:Key="Text0" Color="#F9F0E1"/>
  <SolidColorBrush x:Key="Text1" Color="#F7F3ED"/>
  <SolidColorBrush x:Key="Text2" Color="#9A948C"/>
  <SolidColorBrush x:Key="Text3" Color="#6B6660"/>

  <!-- Accent: wheat -> crust. -->
  <SolidColorBrush x:Key="AccentWheat" Color="#E8C088"/>
  <SolidColorBrush x:Key="AccentCrust" Color="#B4763A"/>
  <LinearGradientBrush x:Key="AccentGradient" StartPoint="0%,0%" EndPoint="100%,100%">
    <GradientStop Color="#F2D6A8" Offset="0"/>
    <GradientStop Color="#E8C088" Offset="0.55"/>
    <GradientStop Color="#B4763A" Offset="1"/>
  </LinearGradientBrush>

  <!-- Ambient glow behind the page, so the black is never dead flat. -->
  <RadialGradientBrush x:Key="AmbientGlow" Center="30%,0%" GradientOrigin="30%,0%" RadiusX="70%" RadiusY="70%">
    <GradientStop Color="#26E8C088" Offset="0"/>
    <GradientStop Color="#00E8C088" Offset="1"/>
  </RadialGradientBrush>

  <!-- Status -->
  <SolidColorBrush x:Key="Good"   Color="#3ECF8E"/>
  <SolidColorBrush x:Key="Warn"   Color="#FFB84D"/>
  <SolidColorBrush x:Key="Danger" Color="#FF6B6B"/>

  <!-- Geometry -->
  <CornerRadius x:Key="RadiusCard">14</CornerRadius>
  <CornerRadius x:Key="RadiusSmall">8</CornerRadius>
  <CornerRadius x:Key="RadiusPill">22</CornerRadius>

  <!-- Type scale -->
  <x:Double x:Key="FontDisplay">44</x:Double>
  <x:Double x:Key="FontTitle">19</x:Double>
  <x:Double x:Key="FontBody">12.5</x:Double>
  <x:Double x:Key="FontLabel">8</x:Double>

  <!-- Spacing scale: 4 / 8 / 12 / 16 / 24 -->
  <Thickness x:Key="SpaceSm">8</Thickness>
  <Thickness x:Key="SpaceMd">12</Thickness>
  <Thickness x:Key="SpaceLg">24</Thickness>
</ResourceDictionary>
```

- [ ] **Step 2: Merge the dictionary into the app**

Replace the contents of `src/Crustcut.App/App.axaml` with:

```xml
<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Crustcut.App.App"
             RequestedThemeVariant="Dark">
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceInclude Source="avares://Crustcut.App/Theme/Tokens.axaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Application.Resources>

  <Application.Styles>
    <FluentTheme />
  </Application.Styles>
</Application>
```

- [ ] **Step 3: Build and commit**

Run: `dotnet build src/Crustcut.App/Crustcut.App.csproj -c Debug --nologo` — expect 0 errors.

```bash
git add src/Crustcut.App/Theme src/Crustcut.App/App.axaml
git commit -m "feat(v0.8b): design tokens — single source for the visual language"
```

---

### Task 4: Shell window — chrome, ambient glow, grouped navigation rail

**Files:**
- Modify: `src/Crustcut.App/MainWindow.axaml` and `.axaml.cs`

**Interfaces:**
- Consumes: `ShellViewModel`, `NavCatalog`, tokens from Task 3.
- Produces: `MainWindow` with a `ContentControl` named `PageHost` that Task 5 fills.

- [ ] **Step 1: Write the window markup**

Replace `src/Crustcut.App/MainWindow.axaml` with:

```xml
<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Crustcut.Presentation;assembly=Crustcut.Presentation"
        xmlns:nav="clr-namespace:Crustcut.Presentation.Navigation;assembly=Crustcut.Presentation"
        x:Class="Crustcut.App.MainWindow"
        x:DataType="vm:ShellViewModel"
        Title="Crustcut"
        Width="1240" Height="780" MinWidth="860" MinHeight="560"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource SurfaceBase}"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaChromeHints="NoChrome"
        ExtendClientAreaTitleBarHeightHint="-1">

  <Window.Styles>
    <!-- Nav rail item. The selected state gets an accent pill on the left edge. -->
    <Style Selector="Button.nav">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Foreground" Value="{DynamicResource Text2}"/>
      <Setter Property="HorizontalAlignment" Value="Stretch"/>
      <Setter Property="HorizontalContentAlignment" Value="Left"/>
      <Setter Property="Height" Value="32"/>
      <Setter Property="Padding" Value="12,0"/>
      <Setter Property="Margin" Value="8,1"/>
      <Setter Property="CornerRadius" Value="{DynamicResource RadiusSmall}"/>
      <Setter Property="FontSize" Value="{DynamicResource FontBody}"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>
    <Style Selector="Button.nav:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource SurfaceHover}"/>
      <Setter Property="TextElement.Foreground" Value="{DynamicResource Text1}"/>
    </Style>
    <Style Selector="Button.nav.active /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource SurfaceCard}"/>
      <Setter Property="TextElement.Foreground" Value="{DynamicResource Text0}"/>
    </Style>

    <Style Selector="TextBlock.groupLabel">
      <Setter Property="FontSize" Value="9"/>
      <Setter Property="Foreground" Value="{DynamicResource Text3}"/>
      <Setter Property="Margin" Value="20,14,0,4"/>
      <Setter Property="LetterSpacing" Value="1.2"/>
    </Style>
  </Window.Styles>

  <Grid ColumnDefinitions="212,*">

    <!-- ===== NAVIGATION RAIL ===== -->
    <Border Grid.Column="0" Background="{DynamicResource SurfaceRail}"
            BorderBrush="{DynamicResource Hairline}" BorderThickness="0,0,1,0">
      <DockPanel>

        <!-- Brand. 56px tall so it shares a baseline with the page eyebrow. -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal"
                    Height="56" Margin="16,0,0,0" Spacing="9">
          <Ellipse Width="16" Height="16" Fill="{DynamicResource AccentGradient}"
                   VerticalAlignment="Center"/>
          <TextBlock Text="Crustcut" FontSize="14" FontWeight="SemiBold"
                     Foreground="{DynamicResource Text1}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Watcher status: flush to the very bottom edge, full width. -->
        <Border DockPanel.Dock="Bottom" Height="44" Padding="16,0"
                Background="#08FFFFFF"
                BorderBrush="{DynamicResource Hairline}" BorderThickness="0,1,0,0">
          <StackPanel Orientation="Horizontal" Spacing="9" VerticalAlignment="Center">
            <Ellipse Width="6" Height="6" Fill="{DynamicResource Good}"/>
            <!-- Repositioned copy: apps, not games. -->
            <TextBlock Text="Watching apps" FontSize="11"
                       Foreground="{DynamicResource Text2}" VerticalAlignment="Center"/>
          </StackPanel>
        </Border>

        <!-- Bottom-anchored utility items sit above the watcher bar. -->
        <ItemsControl DockPanel.Dock="Bottom" ItemsSource="{Binding Bottom}" Margin="0,0,0,8">
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="nav:NavItem">
              <Button Classes="nav" Content="{Binding Label}"
                      Command="{Binding $parent[Window].((vm:ShellViewModel)DataContext).NavigateCommand}"
                      CommandParameter="{Binding Id}"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Primary items, grouped by section. -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,6,0,0">
          <ItemsControl ItemsSource="{Binding Primary}">
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="nav:NavItem">
                <StackPanel>
                  <TextBlock Classes="groupLabel" Text="{Binding Group}"
                             IsVisible="{Binding Group, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                  <Button Classes="nav" Content="{Binding Label}"
                          Command="{Binding $parent[Window].((vm:ShellViewModel)DataContext).NavigateCommand}"
                          CommandParameter="{Binding Id}"/>
                </StackPanel>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </DockPanel>
    </Border>

    <!-- ===== CONTENT ===== -->
    <Grid Grid.Column="1">
      <!-- Ambient glow sits behind the page content. -->
      <Border Background="{DynamicResource AmbientGlow}" IsHitTestVisible="False"/>
      <ContentControl x:Name="PageHost" Margin="24,0,24,24"/>
    </Grid>
  </Grid>
</Window>
```

- [ ] **Step 2: Wire the code-behind**

Replace `src/Crustcut.App/MainWindow.axaml.cs` with:

```csharp
using Avalonia.Controls;
using Crustcut.Presentation;

namespace Crustcut.App;

public partial class MainWindow : Window
{
    public MainWindow() : this(new ShellViewModel()) { }

    public MainWindow(ShellViewModel shell)
    {
        InitializeComponent();
        DataContext = shell;
    }
}
```

- [ ] **Step 3: Build, run, and confirm visually**

Run: `dotnet run --project src/Crustcut.App/Crustcut.App.csproj -c Debug`

Confirm: the rail shows Overview, then PERFORMANCE / GAMING / LEARN section headings, with History and Settings above a "Watching apps" bar flush to the bottom edge. Clicking a nav item highlights it. The window has no OS title bar and the content area carries a soft warm glow from the top-left. Close the window.

- [ ] **Step 4: Commit**

```bash
git add src/Crustcut.App/MainWindow.axaml src/Crustcut.App/MainWindow.axaml.cs
git commit -m "feat(v0.8b): shell window — custom chrome, ambient glow, grouped nav rail

Watcher bar is flush to the bottom edge and reads 'Watching apps' rather than
'Watching games' — gaming is a section now, not the frame."
```

---

### Task 5: Redesigned Overview page

**Files:**
- Create: `src/Crustcut.App/Views/OverviewView.axaml` and `.axaml.cs`
- Create: `src/Crustcut.App/Controls/ScoreArc.cs`
- Create: `src/Crustcut.App/Controls/Sparkline.cs`
- Modify: `src/Crustcut.App/MainWindow.axaml.cs` (set `PageHost.Content`)

**Interfaces:**
- Consumes: tokens from Task 3, `PageHost` from Task 4.
- Produces: `OverviewView`; `ScoreArc` with `double Score` (0–100); `Sparkline` with `IEnumerable<double> Values`.

- [ ] **Step 1: Write `ScoreArc`**

Create `src/Crustcut.App/Controls/ScoreArc.cs`:

```csharp
using Avalonia;
using Avalonia.Controls;
using Avalonia.Media;

namespace Crustcut.App.Controls;

/// <summary>
/// Gradient arc that fills to <see cref="Score"/>. Rendered with a deliberate margin
/// inside its bounds so the glow has room to fall off — clipping it produces a visible
/// square edge, which is exactly the defect the redesign was correcting.
/// </summary>
public sealed class ScoreArc : Control
{
    public static readonly StyledProperty<double> ScoreProperty =
        AvaloniaProperty.Register<ScoreArc, double>(nameof(Score));

    public double Score
    {
        get => GetValue(ScoreProperty);
        set => SetValue(ScoreProperty, value);
    }

    static ScoreArc()
    {
        AffectsRender<ScoreArc>(ScoreProperty);
    }

    public override void Render(DrawingContext ctx)
    {
        var size = Math.Min(Bounds.Width, Bounds.Height);
        if (size <= 0) return;

        const double thickness = 7;
        // Inset by more than half the stroke so the blur/round cap never touches the edge.
        var inset  = thickness;
        var radius = (size - inset * 2) / 2;
        if (radius <= 0) return;

        var centre = new Point(Bounds.Width / 2, Bounds.Height / 2);

        var track = new Pen(new SolidColorBrush(Color.Parse("#12FFFFFF")), thickness);
        ctx.DrawEllipse(null, track, centre, radius, radius);

        var fraction = Math.Clamp(Score, 0, 100) / 100.0;
        if (fraction <= 0) return;

        var brush = new LinearGradientBrush
        {
            StartPoint = new RelativePoint(0, 0, RelativeUnit.Relative),
            EndPoint   = new RelativePoint(1, 1, RelativeUnit.Relative),
            GradientStops =
            {
                new GradientStop(Color.Parse("#F2D6A8"), 0),
                new GradientStop(Color.Parse("#E8C088"), 0.55),
                new GradientStop(Color.Parse("#B4763A"), 1),
            }
        };

        var pen = new Pen(brush, thickness, lineCap: PenLineCap.Round);

        var start = new Point(centre.X, centre.Y - radius);          // 12 o'clock
        var sweep = 2 * Math.PI * fraction;
        var end = new Point(
            centre.X + radius * Math.Sin(sweep),
            centre.Y - radius * Math.Cos(sweep));

        var geometry = new StreamGeometry();
        using (var g = geometry.Open())
        {
            g.BeginFigure(start, false);
            g.ArcTo(end, new Size(radius, radius), 0,
                    isLargeArc: fraction > 0.5, SweepDirection.Clockwise);
            g.EndFigure(false);
        }

        ctx.DrawGeometry(null, pen, geometry);
    }
}
```

- [ ] **Step 2: Write `Sparkline`**

Create `src/Crustcut.App/Controls/Sparkline.cs`:

```csharp
using System.Collections;
using Avalonia;
using Avalonia.Controls;
using Avalonia.Media;

namespace Crustcut.App.Controls;

/// <summary>A filled line chart of recent values. Scales to its own min/max.</summary>
public sealed class Sparkline : Control
{
    public static readonly StyledProperty<IEnumerable?> ValuesProperty =
        AvaloniaProperty.Register<Sparkline, IEnumerable?>(nameof(Values));

    public IEnumerable? Values
    {
        get => GetValue(ValuesProperty);
        set => SetValue(ValuesProperty, value);
    }

    static Sparkline()
    {
        AffectsRender<Sparkline>(ValuesProperty);
    }

    public override void Render(DrawingContext ctx)
    {
        if (Values is null) return;

        var points = new List<double>();
        foreach (var v in Values)
            if (v is IConvertible c) points.Add(c.ToDouble(null));

        if (points.Count < 2 || Bounds.Width <= 0 || Bounds.Height <= 0) return;

        var min = points.Min();
        var max = points.Max();
        var range = max - min;
        if (range <= double.Epsilon) range = 1;   // flat series — draw a level line

        var w = Bounds.Width;
        var h = Bounds.Height;
        var step = w / (points.Count - 1);

        Point At(int i) => new(i * step, h - ((points[i] - min) / range) * h);

        var line = new StreamGeometry();
        using (var g = line.Open())
        {
            g.BeginFigure(At(0), false);
            for (var i = 1; i < points.Count; i++) g.LineTo(At(i));
            g.EndFigure(false);
        }

        var fill = new StreamGeometry();
        using (var g = fill.Open())
        {
            g.BeginFigure(new Point(0, h), true);
            for (var i = 0; i < points.Count; i++) g.LineTo(At(i));
            g.LineTo(new Point(w, h));
            g.EndFigure(true);
        }

        var area = new LinearGradientBrush
        {
            StartPoint = new RelativePoint(0, 0, RelativeUnit.Relative),
            EndPoint   = new RelativePoint(0, 1, RelativeUnit.Relative),
            GradientStops =
            {
                new GradientStop(Color.Parse("#57E8C088"), 0),
                new GradientStop(Color.Parse("#00E8C088"), 1),
            }
        };

        ctx.DrawGeometry(area, null, fill);
        ctx.DrawGeometry(null, new Pen(new SolidColorBrush(Color.Parse("#E8C088")), 1.4), line);
    }
}
```

- [ ] **Step 3: Write the Overview markup**

Create `src/Crustcut.App/Views/OverviewView.axaml`:

```xml
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:Crustcut.App.Controls"
             x:Class="Crustcut.App.Views.OverviewView">

  <UserControl.Styles>
    <Style Selector="Border.card">
      <Setter Property="Background" Value="{DynamicResource SurfaceCard}"/>
      <Setter Property="CornerRadius" Value="{DynamicResource RadiusCard}"/>
      <Setter Property="Padding" Value="14,12"/>
    </Style>
    <Style Selector="TextBlock.label">
      <Setter Property="FontSize" Value="{DynamicResource FontLabel}"/>
      <Setter Property="Foreground" Value="{DynamicResource Text3}"/>
      <Setter Property="LetterSpacing" Value="1.3"/>
    </Style>
    <Style Selector="TextBlock.stat">
      <Setter Property="FontSize" Value="22"/>
      <Setter Property="FontWeight" Value="Normal"/>
      <Setter Property="Foreground" Value="{DynamicResource Text1}"/>
      <Setter Property="Margin" Value="0,3,0,0"/>
    </Style>
  </UserControl.Styles>

  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel>

      <!-- 56px eyebrow shares the rail brand's baseline -->
      <TextBlock Text="YOUR PC" Classes="label" Height="56" VerticalAlignment="Center"/>

      <!-- HERO: score arc + status + actions -->
      <Grid ColumnDefinitions="150,*" Margin="0,0,0,20">
        <Panel Grid.Column="0" Width="150" Height="150">
          <c:ScoreArc Score="{Binding BoostScore}"/>
          <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding BoostScore}"
                       FontSize="{DynamicResource FontDisplay}" FontWeight="Light"
                       Foreground="{DynamicResource Text0}" HorizontalAlignment="Center"/>
            <TextBlock Text="BOOST SCORE" Classes="label" HorizontalAlignment="Center" Margin="0,4,0,0"/>
          </StackPanel>
        </Panel>

        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="24,0,0,0">
          <TextBlock Text="{Binding BoostScoreTier}"
                     FontSize="{DynamicResource FontTitle}" FontWeight="SemiBold"
                     Foreground="{DynamicResource Text0}"/>
          <TextBlock Text="{Binding BoostScoreSubtitle}" FontSize="11"
                     Foreground="{DynamicResource Text2}" Margin="0,6,0,0" TextWrapping="Wrap"/>
          <StackPanel Orientation="Horizontal" Spacing="9" Margin="0,16,0,0">
            <Button Content="Optimize now" Height="34" Padding="20,0"
                    CornerRadius="{DynamicResource RadiusPill}"
                    Background="{DynamicResource AccentGradient}" Foreground="#26190A"
                    FontWeight="SemiBold" Cursor="Hand"/>
            <Button Content="See what changed" Height="34" Padding="16,0"
                    CornerRadius="{DynamicResource RadiusPill}"
                    Background="Transparent" BorderBrush="{DynamicResource Hairline}"
                    BorderThickness="1" Foreground="{DynamicResource Text2}" Cursor="Hand"/>
          </StackPanel>
        </StackPanel>
      </Grid>

      <!-- LIVE STATS with sparklines -->
      <Grid ColumnDefinitions="*,*,*,*" Margin="0,0,0,12">
        <Border Grid.Column="0" Classes="card" Margin="0,0,6,0">
          <StackPanel>
            <TextBlock Text="CPU" Classes="label"/>
            <TextBlock Classes="stat" Text="{Binding CpuPercent, StringFormat={}{0:F0}%}"/>
            <c:Sparkline Height="26" Margin="0,6,0,0" Values="{Binding CpuHistory}"/>
          </StackPanel>
        </Border>
        <Border Grid.Column="1" Classes="card" Margin="6,0">
          <StackPanel>
            <TextBlock Text="MEMORY" Classes="label"/>
            <TextBlock Classes="stat" Text="{Binding RamPercent, StringFormat={}{0:F0}%}"/>
            <c:Sparkline Height="26" Margin="0,6,0,0" Values="{Binding RamHistory}"/>
          </StackPanel>
        </Border>
        <Border Grid.Column="2" Classes="card" Margin="6,0">
          <StackPanel>
            <TextBlock Text="GPU" Classes="label"/>
            <TextBlock Classes="stat" Text="{Binding GpuPercent, StringFormat={}{0:F0}%}"/>
            <c:Sparkline Height="26" Margin="0,6,0,0" Values="{Binding GpuHistory}"/>
          </StackPanel>
        </Border>
        <Border Grid.Column="3" Classes="card" Margin="6,0,0,0">
          <StackPanel>
            <TextBlock Text="NETWORK" Classes="label"/>
            <TextBlock Classes="stat" Text="{Binding NetworkText}"/>
            <c:Sparkline Height="26" Margin="0,6,0,0" Values="{Binding NetHistory}"/>
          </StackPanel>
        </Border>
      </Grid>

    </StackPanel>
  </ScrollViewer>
</UserControl>
```

Create `src/Crustcut.App/Views/OverviewView.axaml.cs`:

```csharp
using Avalonia.Controls;

namespace Crustcut.App.Views;

public partial class OverviewView : UserControl
{
    public OverviewView() => InitializeComponent();
}
```

- [ ] **Step 4: Show it in the shell**

In `src/Crustcut.App/MainWindow.axaml.cs`, after `DataContext = shell;` add:

```csharp
        PageHost.Content = new Views.OverviewView();
```

- [ ] **Step 5: Build, run, confirm visually**

Run: `dotnet run --project src/Crustcut.App/Crustcut.App.csproj -c Debug`

Confirm: a gradient arc surrounds the score with **no visible square edge around its glow**, four stat cards each carry a sparkline, and the pill buttons render. Values will be zero until Task 6 supplies live data. Close the window.

- [ ] **Step 6: Commit**

```bash
git add src/Crustcut.App/Views src/Crustcut.App/Controls src/Crustcut.App/MainWindow.axaml.cs
git commit -m "feat(v0.8b): redesigned Overview — score arc, sparklines, token-driven

Arc is inset inside its bounds so the glow falls off naturally instead of being
clipped into a visible box."
```

---

### Task 6: Host, DI, and live data on Overview

**Files:**
- Modify: `src/Crustcut.App/App.axaml.cs`
- Create: `src/Crustcut.Presentation/OverviewViewModel.cs`
- Test: `src/PrimeOSTuner.Tests/Presentation/OverviewViewModelTests.cs`

**Interfaces:**
- Consumes: `IUiDispatcher`, `AvaloniaDispatcher`, `SystemSampler`, `ActiveTweaksStore`, `IEnumerable<ITweak>`, `IFrameSessionStore`.
- Produces: `OverviewViewModel(SystemSampler, ActiveTweaksStore, IEnumerable<ITweak>, IFrameSessionStore, IUiDispatcher)`.

- [ ] **Step 1: Port the view-model**

Create `src/Crustcut.Presentation/OverviewViewModel.cs` by copying
`src/PrimeOSTuner.UI/ViewModels/DashboardViewModel.cs` and applying exactly these changes:

1. Namespace becomes `Crustcut.Presentation`; class renamed `OverviewViewModel`.
2. Delete `using System.Windows;`.
3. Add an `IUiDispatcher _ui` field, injected as the **last** constructor parameter.
4. Replace every `Application.Current?.Dispatcher.BeginInvoke(X)` with `_ui.Post(X)`.
5. Replace every block of the form
   ```csharp
   var dispatcher = Application.Current?.Dispatcher;
   if (dispatcher is null || dispatcher.CheckAccess()) update();
   else dispatcher.Invoke(update);
   ```
   with `_ui.Post(update);`.
6. Leave `FrameSessionVm` where it is for now and reference it via
   `using PrimeOSTuner.UI.ViewModels;` **only if it compiles**; if it does not, copy
   `FrameSessionVm.cs` into `Crustcut.Presentation` unchanged and use the local copy.

- [ ] **Step 2: Write the tests**

Create `src/PrimeOSTuner.Tests/Presentation/OverviewViewModelTests.cs`:

```csharp
using Crustcut.Presentation;
using FluentAssertions;
using Xunit;

namespace PrimeOSTuner.Tests.Presentation;

/// <summary>An IUiDispatcher that runs inline, which is what makes these view-models testable.</summary>
public sealed class InlineDispatcher : IUiDispatcher
{
    public int PostCount { get; private set; }
    public void Post(Action action) { PostCount++; action(); }
}

public class OverviewViewModelTests
{
    [Fact]
    public void Inline_dispatcher_runs_the_action_immediately()
    {
        var ui = new InlineDispatcher();
        var ran = false;

        ui.Post(() => ran = true);

        ran.Should().BeTrue();
        ui.PostCount.Should().Be(1);
    }
}
```

Extend this file with a construction test once the exact `SystemSampler` and
`ActiveTweaksStore` constructor shapes are confirmed by reading
`src/PrimeOSTuner.UI/App.xaml.cs` DI registrations. Do not fabricate constructor
signatures — read them first.

- [ ] **Step 3: Wire the host**

In `src/Crustcut.App/App.axaml.cs`, replicate the service registrations that
`src/PrimeOSTuner.UI/App.xaml.cs` performs for `SystemSampler`, `ActiveTweaksStore`,
`IFrameSessionStore`, and the tweak catalog. Register
`services.AddSingleton<IUiDispatcher, AvaloniaDispatcher>()` and
`services.AddSingleton<OverviewViewModel>()`. In `OnFrameworkInitializationCompleted`,
resolve `ShellViewModel` and `OverviewViewModel`, construct `MainWindow`, and set
`PageHost.Content = new OverviewView { DataContext = overviewViewModel }`.

- [ ] **Step 4: Verify live data**

Run: `dotnet run --project src/Crustcut.App/Crustcut.App.csproj -c Debug`

Confirm CPU, memory, GPU, and network read non-zero and the sparklines populate over ~30 seconds.

- [ ] **Step 5: Full suite and commit**

Run: `dotnet test src/PrimeOSTuner.Tests/PrimeOSTuner.Tests.csproj --nologo`

```bash
git add -A
git commit -m "feat(v0.8b): Overview runs on live data through IUiDispatcher

Six view-models previously reached for System.Windows.Application.Current.Dispatcher.
The first of them is now framework-free and unit-testable."
```

---

## Verification before moving to Phase 2

- [ ] Full suite green
- [ ] `dotnet build PrimeOSTuner.sln -c Release --nologo` succeeds — **both** apps build
- [ ] The WPF app still runs unchanged (`publish/v0.8a-test/Crustcut.exe`)
- [ ] `grep -rn "System.Windows" src/Crustcut.Presentation src/Crustcut.App` returns nothing
- [ ] No literal hex colour appears in any `.axaml` under `Views/` — only `Controls/` (which renders directly) and `Theme/Tokens.axaml` may contain them

## Deferred to Phase 2

Porting and redesigning the remaining twelve views, the tray icon, the click-through
overlay and global hotkey (both proven viable by the spike documented at
`docs/performance/2026-07-26-avalonia-overlay-spike.md`), single-instance handling,
elevation and autostart for the new executable, publish validation, and finally deleting
`PrimeOSTuner.UI` together with its four view-model test files.
